<h1>Listing Confessions</h1>

<table cellspacing="20">
  <thead>
    <tr>
      <th>Name</th>
      <th>Confess</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= post.Confess %></td>
        <!--<td><%= link_to 'Reveal', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Confession', new_post_path %>
